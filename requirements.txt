# Author: 晨星
# WorldAI runtime dependencies (ranges). For bit-exact reproducibility use
# requirements.lock.txt. All packages ship win_amd64 wheels - no compiler needed.
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.5
numpy>=1.26
rank-bm25>=0.2.2
pypdf>=6.19.0
httpx>=0.27
loguru>=0.7
# Production providers (optional at runtime, selected via env):
faiss-cpu>=1.15.1
fastembed>=0.4
llama-cpp-python>=0.3.35
# Dev/test: requirements-dev.txt (pytest)
